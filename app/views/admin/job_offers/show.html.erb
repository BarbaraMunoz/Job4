<h1><%= @job_offer.title %></h1>

<p><strong>Descripción:</strong></p>
<p><%= @job_offer.description %></p>

<p><strong>Requerimientos:</strong></p>
<p><%= @job_offer.requirements %></p>

<%= link_to 'Editar', edit_admin_job_offer_path(@job_offer) %> |  <%= button_to 'Eliminar', admin_job_offer_path(@job_offer), method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to raw('<i class="fas fa-arrow-left"></i> Volver'), :back, class: "admin-btn" %>
